<?xml version="1.0" encoding="UTF-8"?>
<hibernate-configuration>
    <session-factory>
        <!-- Database connection settings -->
        <property name="connection.driver_class">com.mysql.jdbc.Driver</property>
        <!-- Location  B.D. -->
        <property name="connection.url">jdbc:mysql://localhost:3306/test_bd</property>
        <!-- User the B.D. -->
        <property name="connection.username">root</property>
        <!-- Pass the B.D. -->
        <property name="connection.password">Apolonia74</property>
        

        <!-- Dialect SQL -->
        <property name="dialect">org.hibernate.dialect.MySQLDialect</property>

        <!--Show in console operations SQL -->
        <property name="show_sql">true</property>
         
        <!-- Use XML-based mapping-->	
        <mapping resource="org/apb/pojo/Tramite.hbm.xml"/>
        <!-- Use mapping based on annotations -->
        
    </session-factory>
</hibernate-configuration>